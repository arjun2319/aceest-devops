apiVersion: v1
kind: Service
metadata:
  name: aceest-service
  namespace: default
spec:
  selector:
    app: aceest-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort